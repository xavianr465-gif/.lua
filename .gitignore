-- [[ FORSAKEN MOBILE ENGINE - FIXED & KEYLESS ]] --
repeat wait() until game:IsLoaded()

print("--- ATTEMPTING SYNC-LOAD ---")

-- BYPASSING THE KEY SYSTEM ENTIRELY
getgenv().script_key = "XKrZDyqLLdJwLfnfvjhkcyfkTNZWZyxH"
getgenv().KeyVerified = true
getgenv().KeyEntered = true

getgenv().autogenSettings = {
    PUZZLES_BEFORE_REJOIN = 10,
    PLAYERS_BEFORE_START = 2,
    MAXIMUM_TIMER_SECONDS = 120,
    MINIMUM_TIMER_SECONDS = 3,
    RECENT_SERVER_BLOCKLIST_LENGTH = 5,
    PLAYER_TIMER_CHECK = 1,
    SWAP_ON_MS4 = true,
    SKIP_LAST_PUZZLE = true, -- Safety
    DISABLE_RENDERING = true, -- Set to true to stop mobile crashes
    AUTO_HIDE_ABILITY = true,
    RUN_ONLY_WHEN_COOLKIDD = false,
    WEBHOOK = '',
}

-- THE LOADER (FIXED URL)
local url = 'https://raw.githubusercontent.com/who-else123/my-scripts/refs/heads/main/Syncsaken/auto-autogen.lua'

local success, result = pcall(function()
    return game:HttpGet(url)
end)

if success and result then
    print("--- SUCCESS: STARTING SCRIPT ---")
    -- We wrap this in another pcall to stop errors from crashing the menu
    pcall(function()
        loadstring(result)()
    end)
else
    print("--- ERROR: LINK DEAD OR BLOCKED ---")
    warn("The source link is currently 404.")
end
