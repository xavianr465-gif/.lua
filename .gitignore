-- // Configuration Settings
getgenv().autogenSettings = {
    PUZZLES_BEFORE_REJOIN = 15, 
    PLAYERS_BEFORE_START = 3, 

    MAXIMUM_TIMER_SECONDS = 120, 
    MINIMUM_TIMER_SECONDS = 3, 

    RECENT_SERVER_BLOCKLIST_LENGTH = 5, 
    PLAYER_TIMER_CHECK = 1, 

    SWAP_ON_MS4 = true, 
    SKIP_LAST_PUZZLE = false, 
    DISABLE_RENDERING = false, 
    AUTO_HIDE_ABILITY = true, 
    RUN_ONLY_WHEN_COOLKIDD = false, 
    WEBHOOK = "", -- Paste your Discord Webhook URL here if needed
}

-- // The Execution Script (The part that makes it actually run)
-- Note: Ensure you are using a stable executor for this.
loadstring(game:HttpGet("https://raw.githubusercontent.com/xavianr465-gif/.lua/main/script.lua"))()
